<div id="clearance" class="sign-up">
  <h2><%= t(".title") %></h2>

  <%= form_for @user do |form| %>
    <%= render partial: '/users/signupform', object: form %>
  <% end %>
  
</div>

<%= link_to "Sign in with Facebook", "/auth/facebook" %>
